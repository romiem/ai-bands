{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Artist",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "The unique identifier for the artist or band"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "The name of the artist or band"
    },
    "dateAdded": {
      "type": "string",
      "format": "date-time",
      "description": "Date this artist was added to the database (ISO 8601 UTC format)"
    },
    "dateUpdated": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time",
      "description": "Date this entry was last updated (ISO 8601 UTC format), or null if never updated"
    },
    "comments": {
      "type": [
        "string",
        "null"
      ],
      "description": "Additional notes or context about why this artist is considered AI-generated, or null if no comments"
    },
    "tags": {
      "type": "array",
      "description": "Special tags/flags for this artist",
      "items": {
        "type": "string",
        "enum": [
          "ai-visuals",
          "anonymous",
          "audio-artifacts",
          "high-output",
          "human-lyrics",
          "inconsistent-style",
          "minimal-bio",
          "no-social-presence",
          "partial-ai"
        ]
      },
      "uniqueItems": true
    },
    "spotify": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^[a-zA-Z0-9]{22}$",
      "description": "Spotify artist ID, or null if not available"
    },
    "apple": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^[0-9]+$",
      "description": "Apple Music artist ID, or null if not available"
    },
    "amazon": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^[A-Z0-9]+$",
      "description": "Amazon profile ID, or null if not available"
    },
    "youtube": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^(UC[a-zA-Z0-9_-]{22}|@([a-zA-Z0-9_.-]|%[0-9A-Fa-f]{2})*)$",
      "description": "YouTube channel handle/ID or null if not available"
    },
    "tiktok": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^@[a-z0-9_.]{2,24}$",
      "description": "TikTok profile handle, or null if not available"
    },
    "instagram": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^[a-z0-9_.]{1,30}$",
      "description": "Instagram profile handle, or null if not available"
    },
    "urls": {
      "type": "array",
      "description": "Supporting evidence URLs (e.g., Reddit posts, blog articles, etc.)",
      "items": {
        "type": "string",
        "format": "uri"
      }
    }
  },
  "required": [
    "id",
    "name",
    "dateAdded",
    "dateUpdated",
    "comments",
    "tags",
    "spotify",
    "apple",
    "amazon",
    "youtube",
    "tiktok",
    "instagram",
    "urls"
  ],
  "additionalProperties": false
}